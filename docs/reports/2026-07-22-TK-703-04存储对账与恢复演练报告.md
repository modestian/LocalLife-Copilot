# TK-703-04 存储对账与恢复演练报告

## 1. 验收范围

本报告对应 `TK-703-04`：实现 MySQL/OpenSearch 对账和全量重建，并完成数据库备份恢复、Redis 丢失回源与索引重建演练。

## 2. 已实现的门禁

- 对账以 MySQL 当前有效 Chunk 为事实源，报告缺失、逻辑重复、孤儿、内容漂移和非确定性投影 ID。
- 重建写入新索引并在切换别名前执行精确对账；差异存在、事实集为空或目标索引已存在时失败关闭。
- MySQL 备份采用单事务快照，并以 AES-256-CBC + PBKDF2 加密；恢复到隔离数据库后逐表精确比对行数，不覆盖事实库。
- Redis 演练要求显式确认，清空热缓存后验证持久消息从 MySQL 恢复且缓存重新填充。

## 3. 自动化验证

核心自动化覆盖精确一致、缺失、重复、孤儿、内容漂移、非确定性 ID、可信范围覆盖、空事实源拒绝、重建失败不切别名和成功后原子切换。既有 Redis 自动化同时覆盖缓存缺失、Redis 故障、损坏缓存和所有权校验后的 MySQL 回源。

专项测试结果：`11 passed`；Ruff lint、Ruff format check、PowerShell 语法和 Shell 脚本容器执行均通过。

## 4. 完整演练结果

演练于 `2026-07-22T07:24:04Z` 在 Docker Compose 演示环境执行，MySQL 8.4、Redis 7.4、OpenSearch 3.7.0 和 API 均处于健康状态。

| 演练项 | 实测结果 | 判定 |
| --- | --- | ---: |
| MySQL 加密备份 | `mysql-20260722T072404Z.sql.enc`，SHA-256 `6faa65138e6a7dffe9139003afc0988044e64cdbc6af8cef42affca8e2357135` | 通过 |
| 隔离恢复与对账 | 恢复到 `local_life_restore_drill_20260722t072404z`，31 张表逐表精确行数一致 | 通过 |
| 恢复库清理 | 演练后不存在 `local_life_restore_drill_%` 数据库，事实库未被覆盖 | 通过 |
| 重建前对账 | MySQL 2 条、OpenSearch 2 条，零差异 | 通过 |
| 全量索引重建 | 新索引 `local-life-chunks-rebuild-20260722t072404z` 写入并校验 2 条 | 通过 |
| 重建后对账 | 缺失、重复、孤儿、内容漂移、非确定性 ID 均为 0 | 通过 |
| 原子别名切换 | `local-life-documents-read/write` 均指向新索引，write alias 标记为写索引 | 通过 |
| Redis 丢失回源 | 清空 DB 0 后从 MySQL 恢复 1 条消息，消息 ID 一致且缓存重新填充 | 通过 |

加密密码仅存在于演练进程环境，执行结束后已清除；明文 SQL 从未写入宿主机。失败演练遗留的隔离数据库和容器临时文件已按精确名称清理。

## 5. 结论

MySQL 加密备份恢复、Redis 丢失回源和 OpenSearch 全量重建均在真实 Compose 依赖上完成，重建后投影与 MySQL 事实源一致，未发现缺失、重复或孤儿索引。

`TK-703-04` 判定：**完成**。

可复现步骤见[存储对账与恢复演练手册](../development/TK-703-04-存储对账与恢复演练.md)。原始 JSON、Markdown 和加密备份证据保存在本机 `artifacts/tk-703-04/`，该目录按规范不提交 Git。
