# TK-703-05 发布门禁验收记录

## 1. 记录信息

| 项目 | 内容 |
| --- | --- |
| Task | `TK-703-05` 汇总发布门禁并签署验收记录 |
| Story / Epic | `ST-703` / `EP-07` |
| 门禁负责人 | L |
| 记录日期 | 2026-07-22 |
| 验收基线 | `1efa927`（包含 TK-703-04；合入 `main` 后应以发布候选 Commit 重新复核） |
| 判定规则 | P0 未清零、存在高危安全问题或必需指标缺少可信实测时，失败关闭并禁止发布 |
| 最终决定 | **拒绝发布（NOT APPROVED）** |

本记录依据《大众点评 AI 智能助手：史诗、故事、任务与人员分工》中 `ST-703` 的七项验收准则，汇总 TK-703-01～04 的可复现报告。记录“拒绝发布”是一次有效门禁签署，不代表 TK-703-05 未执行；在阻断项关闭前不得把本记录改写为“通过”。

## 2. 门禁汇总

| 门禁 | 验收要求 | 证据 | 结果 |
| --- | --- | --- | ---: |
| 后端测试与覆盖率 | 核心领域和服务层覆盖率不低于 70% | [TK-703-01 后端测试与覆盖率报告](./2026-07-22-TK-703-01后端测试与覆盖率报告.md)：916 通过、0 失败；全应用分支覆盖率 74.80%，核心分层覆盖率均高于 70% | 通过 |
| 接口异常与安全 | 401/403/404/409/422/429、依赖故障和安全专项有自动化证据；无阻断/高危安全缺陷 | [TK-703-01 报告](./2026-07-22-TK-703-01后端测试与覆盖率报告.md)与[TK-703-02 安全专项报告](./2026-07-22-TK-703-02安全专项验收报告.md)：专项 79 通过、2 条条件跳过，完整回归 932 通过、39 条条件跳过，未发现阻断或高危安全问题 | 通过 |
| 性能 | API P95 `< 500 ms`、混合检索 P95 `< 300 ms`、SSE 首包 P95 `≤ 2 s` | [TK-703-03 Locust 性能门禁报告](./2026-07-22-TK-703-03-Locust性能门禁报告.md)：完整服务环境不可用，三项均为 0 样本、P95 为 N/A | **阻断** |
| 存储对账与恢复 | 识别缺失、重复、孤儿索引；完成 MySQL 恢复、Redis 回源和 OpenSearch 全量重建 | [TK-703-04 存储对账与恢复演练报告](./2026-07-22-TK-703-04存储对账与恢复演练报告.md)：31 张表恢复一致，索引差异为 0，Redis 回源成功 | 通过 |

四项门禁中三项通过、一项阻断。性能实现、判定器和失败关闭演练已经完成，但不能用工具可运行、单元测试通过或其他服务层延迟数据替代 Locust 对发布候选真实 HTTP 链路的 P95 实测。

## 3. P0/P1 缺陷登记

本表列出本次 `ST-703` 门禁证据中所有尚未清零的 P0/P1 项；已关闭问题保留在各专项报告和 Git 历史中，不重复登记为开放缺陷。

| ID | 优先级 | 状态 | 缺陷与风险 | 关闭条件 | 负责人 |
| --- | --- | --- | --- | --- | --- |
| `GATE-703-P0-001` | P0 | OPEN / BLOCKING | 发布候选环境没有 API、混合检索和 SSE 首包的 Locust P95 实测；无法证明三个性能阈值达标 | 在完整 Compose 演示环境、确定性数据和发布候选 Commit 上执行 `scripts/run_tk703_performance.ps1`；三项场景均有足量样本、失败请求为 0 且自动判定均为 `PASS`；将原始产物归档并更新 TK-703-03 报告 | L |

当前门禁证据未发现其他开放 P0/P1 安全或恢复缺陷。`GATE-703-P0-001` 未关闭，因此 P0 未清零。

## 4. 验收签署

| 签署角色 | 签署决定 | 理由 | 状态 |
| --- | --- | --- | --- |
| ST-703 Owner（L） | **拒绝发布** | 性能门禁缺少可信实测，`GATE-703-P0-001` 仍为开放 P0 | 已形成门禁记录，禁止签署通过 |

发布状态：**BLOCKED / NOT APPROVED**。

本记录随 Git Commit 固化并作为当前签署凭据。负责人复核提交内容后，Commit 的作者、时间和哈希构成可追溯记录；本文件不伪造手写签名或把未执行的性能测试标记为通过。

## 5. 解禁与重新签署步骤

1. 合并 TK-703-04，并在最终发布候选 Commit 上启动完整 Compose 环境和确定性演示数据。
2. 在仓库根目录执行：

   ```powershell
   $env:PERF_USERNAME = "demo-admin"
   $env:PERF_PASSWORD = Read-Host "Performance account password"
   .\scripts\run_tk703_performance.ps1
   Remove-Item Env:PERF_PASSWORD -ErrorAction SilentlyContinue
   ```

3. 核对 `artifacts/tk-703-03/` 中的 Locust CSV、HTML、JSON 和 Markdown：三个业务场景样本充足、失败数为 0，自动门禁均为 `PASS`。
4. 更新 TK-703-03 报告的实测环境、发布候选 Commit、样本量和 P95；关闭 `GATE-703-P0-001`。
5. 重新执行与发布候选变更风险相匹配的后端、安全和恢复回归；若没有新的开放 P0/P1 或高危安全问题，再新增一条重新签署记录并将发布决定改为 `APPROVED`。不得覆盖或删除本次拒绝记录。

## 6. 结论

`TK-703-05` 的门禁汇总和验收决定已经形成。由于性能 P0 阻断未关闭，当前发布候选**不得交付或签署通过**；解禁必须以真实环境的 Locust 原始结果和自动门禁通过为依据。
